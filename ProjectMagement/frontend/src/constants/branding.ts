/** Product name — same for every tenant (Exodus PM platform). */
export const PRODUCT_NAME = 'Exodus PM'

/** Shown under the product name on login and in browser title. */
export const PRODUCT_TAGLINE = 'Project management platform'

/** Sidebar collapsed label (keep short). */
export const PRODUCT_MARK = 'EP'
