# REST base URL (optional). If unset, requests use /api and Vite dev server proxies to the backend.
# Use a full URL when the app and API are on different origins (production or direct-to-8081 in dev):
# VITE_API_URL=http://localhost:8081/api
