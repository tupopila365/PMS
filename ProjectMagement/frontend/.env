# Vite proxies /api -> http://localhost:8081 (see vite.config.ts).
# Optional: call backend directly (browser uses CORS). Dev default is relative /api via proxy.
# VITE_API_URL=http://localhost:8081/api
